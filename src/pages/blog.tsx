import React from 'react';
import { Header } from '../components/Header';

const Blog: React.FC = () => {
  return (
    <div style={{ width: '100%' }}>
      <Header />
    </div>
  );
};

export default Blog;
